rootProject.name = 'dropwizard-guicey-examples'

include 'autoconfig-base',
        'manualconfig-base',
        'autoconfig-installer',
        'manualconfig-installer',
        'bundle-base',
        'hibernate'